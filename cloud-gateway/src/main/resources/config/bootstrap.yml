server:
    port: 80
spring:
    application:
        name: @project.name@
    profiles:
        active: @env@
    cloud:
        gateway:
            discovery:
                locator:
                    lower-case-service-id: true
                    enabled: true
                    filters:
                        - StripPrefix=1
